<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".ReadMore">

    <!-- Include your app bar layout -->
    <include layout="@layout/appbar"
        android:id="@+id/tb"/>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:padding="16dp">

        <!-- LinearLayout inside ScrollView -->
        <LinearLayout
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">
            <TextView
                android:id="@+id/title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:padding="10dp"
                android:textStyle="bold"
                android:textSize="20sp"
                android:gravity="center"
                />


            <!-- Image representing paper recycling -->
            <ImageView
                android:id="@+id/recycling_image"
                android:layout_width="300dp"
                android:layout_height="400dp"
                android:layout_gravity="center"
                android:contentDescription="Paper Recycling Process Image"
                android:scaleType="fitXY" />

            <!-- Detailed description of the recycling process -->
            <TextView
                android:id="@+id/recycling_process"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text=""
                android:textStyle="bold"
                android:textSize="18sp"
                android:lineSpacingExtra="8dp"
                android:layout_marginTop="25dp"
                android:layout_gravity="center"
                android:paddingEnd="8dp"
                android:paddingStart="8dp" />

        </LinearLayout>
    </ScrollView>

</LinearLayout>
